#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../react-native-tizen-dotnet/build/bin/react-native-tizen-scripts.js" "$@"
  ret=$?
else 
  node  "$basedir/../react-native-tizen-dotnet/build/bin/react-native-tizen-scripts.js" "$@"
  ret=$?
fi
exit $ret
