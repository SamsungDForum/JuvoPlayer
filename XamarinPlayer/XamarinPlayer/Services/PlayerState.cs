namespace XamarinMediaPlayer.Services
{
    public enum PlayerState
    {
        Idle,
        Preparing,
        Prepared,
        Stopped,
        Paused,
        Playing,
        Error
    }
}
