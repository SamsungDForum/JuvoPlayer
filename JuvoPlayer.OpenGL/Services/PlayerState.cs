namespace JuvoPlayer.OpenGL.Services {
    public enum PlayerState
    {
        Idle = 0,
        Preparing = 1,
        Prepared = 2,
        Stopped = 3,
        Paused = 4,
        Playing = 5,
        Error = 6
    }
}
